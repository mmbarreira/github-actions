name: First Workflow

on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print Greeting
        run: |
            echo "Hello World!"
            echo "How you doing..?"
            echo "How you doing2..?"
        
      - name: Print Goodbye
        run: echo "Hasta..."
